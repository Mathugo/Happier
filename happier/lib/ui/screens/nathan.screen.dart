import 'package:flutter/material.dart';

class NathanScreen extends StatelessWidget {
  const NathanScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('TO BE IMPLEMENTED!'),
    );
  }
}
